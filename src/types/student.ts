export interface Student {
  id: string
  name: string
  grade: string
  birthDate: string
  parentName: string
  parentPhone: string
  parentEmail: string
  enrollmentIds: string[]
  createdAt: string
}
